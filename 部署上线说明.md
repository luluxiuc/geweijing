# 歌未竟 · 部署上线说明（个人非商用，无需备案）

部署包：`部署包-歌未竟.zip`（已解压内含 `index.html`、`d/`、`m/`、`assets/`、`data/`、`images/`、`audio/`，解压后根目录直接可用，相对路径，部署到任何子路径都能跑）。

> 备案提示：使用境外平台（Cloudflare Pages / Netlify / GitHub Pages / Vercel）**不需要 ICP 备案**；只有放在中国大陆服务器（阿里云/腾讯云国内节点）才需要备案。个人纪念站推荐境外平台。

## 方式一：Cloudflare Pages（推荐 · 免费 · HTTPS · 国内可访问性相对最好）

1. 打开 https://dash.cloudflare.com 注册/登录（邮箱即可，免费）。
2. 左侧菜单：**Workers & Pages → Create → Pages → Upload assets**（上传资产）。
3. 把 `部署包-歌未竟.zip` **解压**，得到一个包含 `index.html` 的文件夹；把整个文件夹拖进上传框。
4. 保持默认（Build settings 不需要）→ **Deploy**。
5. 部署完成，你会得到一个 HTTPS 地址 `https://xxxx.pages.dev`——手机扫码/输入即访问，自动识别设备进手机版。
6. 以后更新：控制台里重新上传新的 dist 包即可（或绑定 GitHub 仓库自动构建）。

## 方式二：Netlify Drop（最简单 · 已注册可直接拖拽）

1. 打开 https://app.netlify.com/drop
2. 把解压后的 dist 文件夹拖进去，即得 `https://xxx.netlify.app`。
3. 国内访问速度一般，不如 Cloudflare。

## 方式三：GitHub Pages（适合已有 GitHub 账号）

1. 新建仓库（如 `geweijing`）→ 上传解压后的 dist 内容。
2. 仓库 **Settings → Pages → Source: Deploy from a branch → main → /(root)**。
3. 几分钟后得到 `https://你的用户名.github.io/geweijing/`。国内访问有时不稳定。

## 方式四：想放国内服务器（如腾讯云/阿里云海外节点 OSS）

个人推荐上面三种即可；如你已有云账号，把 dist 上传到**香港/新加坡节点**的 COS/OSS 静态网站托管 + 自定义域名，同样无需备案，速度对国内更好。需要的话我出详细步骤。

## 上线后建议（可之后再做）

- 域名：有钱可注册一个域名（如 `gewiejing.cn`）绑定到 Pages；不注册用免费 `.pages.dev` 也完全可以。
- PWA（添加到手机主屏幕 + 离线可用）、图片 WebP 优化、收藏卡（og 标签）等，等正式域名定了再一起做。
- 更新的标准流程：`cd site && npm run build` → 把新 `site/dist` 重新打包上传。

## 常见问题

- 上传后 404：检查解压后的文件夹里第一层就是 `index.html`（不要把 dist 文件夹套在更深层）。
- 手机访问自动进 `/m/`，电脑进 `/d/`：根 `index.html` 会按设备跳转，无需手动。
- 换页「丝滑」转场：已内置（Chrome/Edge/Safari 生效，其他浏览器自动降级，不影响功能）。
